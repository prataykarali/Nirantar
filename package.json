{
  "name": "nirantar-vercel",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm ci && npm run build",
    "vercel-build": "cd frontend && npm ci && npm run build"
  },
  "engines": {
    "node": "22.x"
  }
}
