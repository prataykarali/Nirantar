# Module 5 — Dhara (Decide)

Queues, shedding, inventory protection. The only plane that changes admission.

This directory **is** the `orchestrator` package (`decision_engine/`, `scheduling/`, `resilience/`, `graph/`).
