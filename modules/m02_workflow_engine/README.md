# Module 2 — Workflow engine

Compiles language into actions and restricted tools. The LLM does not admit traffic.

- `python/` — workflow package
- `adapters/` — public-service adapters (`RailwayAdapter` lives here as it grows)
