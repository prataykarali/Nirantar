{
  "name": "nirantar-backend-vercel-bridge",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "install": "cd ../frontend && npm install",
    "build": "cd ../frontend && npm run build",
    "vercel-build": "cd ../frontend && npm run build"
  }
}
