# Code Standards & Quality Gates

## Requirements
- Python code must conform to PEP8 with strict type annotations.
- Maximum nesting depth <= 3 (Never Nester principle).
- TypeScript frontend must adhere to strict mode with zero unhandled any types.
- All new features must include corresponding unit and integration tests passing in Pytest.
